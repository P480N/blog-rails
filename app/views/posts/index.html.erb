<% @posts.each do |post| %>
	<div class="blog-post">
	  <h2 class="blog-post-title"><%= link_to post.title, post %></h2>
	  <% if user_signed_in? %>
	  	<%= post.body.html_safe %> - <%= link_to "Eliminar", post, method: :delete %>
	  <% end %>
	</div>
<% end %>