<% @posts.each do |post| %>
	<div class="blog-post">
	  <h2 class="blog-post-title"><%= post.title %></h2>
	  <%= post.body.html_safe %> - <%= link_to "Eliminar", post, method: :delete %>
	</div>
<% end %>